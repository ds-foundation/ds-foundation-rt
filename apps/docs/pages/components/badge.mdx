---
title: Badge
---

# Badge

Compact pill label for displaying statuses, categories, and counts.

## Usage

```tsx
import { Badge } from '@ds-foundation/react';

<Badge>Active</Badge>
<Badge color="neutral" variant="subtle">Draft</Badge>
<Badge color="danger" variant="subtle">Blocked</Badge>
<Badge color="success" variant="subtle">Completed</Badge>
<Badge color="warning" variant="subtle">Pending</Badge>
<Badge color="info" variant="subtle">New</Badge>
<Badge color="neutral" variant="outline">Tag</Badge>
<Badge variant="pink">Featured</Badge>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "secondary" \| "destructive" \| "outline" \| "success" \| "warning" \| "info" \| "pink"` | `"default"` | Visual style and semantic color |
| `...rest` | `HTMLAttributes<HTMLDivElement>` | — | Forwarded to the root `<div>` |

## Accessibility

- Renders as a `<div>` — not interactive by default
- Always pair a color variant with a meaningful text label (WCAG 1.4.1 — use of color)
- For dynamically updating counts (e.g. notifications), wrap in a container with `aria-live="polite"`
