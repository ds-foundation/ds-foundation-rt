---
id: accordion
type: component
version: 1.0.0
status: stable
variants: []
sizes: []
adapters:
  tailwind:
    default:
      base: "border-b"
      trigger: "flex flex-1 items-center justify-between py-4 font-medium transition-all hover:underline [&[data-state=open]>svg]:rotate-180"
      content: "overflow-hidden text-sm transition-all data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down"
accessibility:
  role: "region"
  wcag:
    - "1.3.1 Info and Relationships"
    - "2.1.1 Keyboard"
    - "4.1.2 Name, Role, Value"
  aria:
    - "aria-expanded on trigger reflects open state"
    - "aria-controls linking trigger to panel"
    - "role=region on content panel"
  notes: "Built on Radix AccordionPrimitive. Keyboard: Space/Enter toggles, Arrow keys navigate between items."
ai-prompt: "Accordion renders a set of vertically stacked collapsible sections. Use it for FAQs, settings panels, or any content where showing everything at once would overwhelm the user. Set type='single' for exclusive open (only one section open at a time) with collapsible=true to allow all closed. Set type='multiple' to allow several sections open simultaneously. Compose with AccordionItem, AccordionTrigger, and AccordionContent. Each AccordionItem requires a unique value prop."
---

# Accordion

Vertically stacked collapsible sections built on Radix UI AccordionPrimitive.

## Usage

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from '@ds-foundation/react';

<Accordion type="single" collapsible>
  <AccordionItem value="item-1">
    <AccordionTrigger>Section title</AccordionTrigger>
    <AccordionContent>Section body content.</AccordionContent>
  </AccordionItem>
  <AccordionItem value="item-2">
    <AccordionTrigger>Another section</AccordionTrigger>
    <AccordionContent>More content here.</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Props

### Accordion (Root)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `"single" \| "multiple"` | — | Whether one or many items can be open at once |
| `collapsible` | `boolean` | `false` | When `type="single"`, allows all items to be closed |
| `defaultValue` | `string \| string[]` | — | Initially open item(s) |
| `value` | `string \| string[]` | — | Controlled open item(s) |
| `onValueChange` | `(value: string \| string[]) => void` | — | Callback when open state changes |

### AccordionItem

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Unique identifier for this item (required) |
| `disabled` | `boolean` | `false` | Prevents the item from being toggled |

### AccordionTrigger / AccordionContent

Accept all standard HTML attributes plus `className` for style overrides.

## Accessibility

- Built on Radix AccordionPrimitive — ARIA roles, `aria-expanded`, and `aria-controls` are managed automatically
- Keyboard: `Space` / `Enter` toggles the focused item; `Arrow Up/Down` moves focus between triggers
- Each content panel receives `role="region"` linked to its trigger via `aria-labelledby`
